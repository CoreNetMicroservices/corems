<Project Sdk="Microsoft.NET.Sdk.Web">

  <PropertyGroup>
    <RootNamespace>CoreMs.TranslationMs.Api</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <ProjectReference Include="..\CoreMs.TranslationMs.Core\CoreMs.TranslationMs.Core.csproj" />
    <ProjectReference Include="..\CoreMs.TranslationMs.Infrastructure\CoreMs.TranslationMs.Infrastructure.csproj" />
  </ItemGroup>

  <!-- Local development: ProjectReference (default) -->
  <ItemGroup Condition="'$(UsePackageReferences)' != 'true'">
    <ProjectReference Include="$(SolutionRoot)aspire\CoreMs.ServiceDefaults\CoreMs.ServiceDefaults.csproj" />
  </ItemGroup>

  <!-- Docker/CI builds: PackageReference -->
  <ItemGroup Condition="'$(UsePackageReferences)' == 'true'">
    <PackageReference Include="CoreMs.ServiceDefaults" />
  </ItemGroup>

</Project>
