max_replicas = 5

image_tags = {
  user-ms          = "latest"
  communication-ms = "latest"
  document-ms      = "latest"
  translation-ms   = "latest"
  template-ms      = "latest"
}

# Custom domains are optional and NOT configured here. To attach a custom domain,
# run infra/scripts/setup-custom-domain.sh after deployment. See infra/scripts/README.md.
